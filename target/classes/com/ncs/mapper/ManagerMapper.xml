<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.ncs.mapper.ManagerMapper">
  	<select id="selectList" resultType="com.ncs.vo.ManagerDTO">
  		select seq, userid, username, joindate from user where exist=1 order by seq desc
  	</select>
  
  </mapper>